// This is a temporary file to avoid Vite build errors
// We'll fix the original file later

export function PolicySignupForm() {
  return (
    <div>Form temporarily disabled for development</div>
  );
}